# Demo: Preview Deployments

This file exists only to demonstrate Vercel's automatic preview-deployment flow:
opening this as a pull request against `main` triggers a preview build, and
Vercel comments the live preview URL directly on the PR — before anything
touches production.

Safe to close/delete this PR without merging once the demo is done.
